apiVersion: v1
kind: Service

metadata:
  name: backend
  namespace: find-my-ride

spec:
  selector:
    app: backend

  type: NodePort

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080